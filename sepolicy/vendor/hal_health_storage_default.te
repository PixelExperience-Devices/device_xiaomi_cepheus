# Allow idle-maint to read metadata dir
r_dir_file(hal_health_storage_default, metadata_file)

# Allow idle-maint to read dt_firmware dir
r_dir_file(hal_health_storage_default, sysfs_dt_firmware_android)

# Allow idle-maint to read manual GC
allow hal_health_storage_default sysfs_scsi_devices_0000:file rw_file_perms;
